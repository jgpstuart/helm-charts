apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quartz-blog.fullname" . }}-test-connection"
  labels:
    {{- include "quartz-blog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quartz-blog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
