apiVersion: v1
kind: Service
metadata:
  name: {{ include "quartz-blog.fullname" . }}
  labels:
    {{- include "quartz-blog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quartz-blog.selectorLabels" . | nindent 4 }}
